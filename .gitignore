# Данни по време на работа (създават се автоматично от приложението)
admin/data/orders.json
admin/data/visits.json

# Production secrets — копие от admin/config.example.php на сървъра
admin/config.php

# Медия (~3 GB) — на сървъра, не в GitHub
images/

# Системни / редакторски файлове
.DS_Store
Thumbs.db
desktop.ini
*.log

# Временни инструменти
_*.php
