/* =============================================================
   Локален преглед — празно = images/ от същата папка
   Production: deploy копира config.production.js → config.js
   CDN вариант: виж config.cdn.js
   ============================================================= */
window.SITE_ASSET_BASE = "";
